# Deploy-time config for upload_production. Copy to production.deploy
# and fill in. Sourced locally; never ships to the remote.

SERVER_SSH=user@hostname
SERVER_SSH_KEY=/path/to/ssh/key
SERVER_SSH_DIR=/opt/bitbang
